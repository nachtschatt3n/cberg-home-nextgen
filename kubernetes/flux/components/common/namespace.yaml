---
apiVersion: v1
kind: Namespace
metadata:
  name: not-used
  annotations:
    kustomize.toolkit.fluxcd.io/prune: disabled
  # PSA labels applied per-namespace via kustomize patches: in each
  # kubernetes/apps/<ns>/kustomization.yaml. Common template carries no PSA
  # labels so kustomize component-merge doesn't override the per-NS choice.
  # See docs/sops/talos-upgrade.md for the 2026-04-30 incident root cause.
