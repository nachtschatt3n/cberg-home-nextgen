---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: my-software-development
components:
  - ../../flux/components/common
resources:
  - ./absenty/ks.yaml
  - ./andreamosteller/ks.yaml
  - ./opencode-andreamosteller/ks.yaml
# PSA = baseline (cluster default). Inline Namespace resource was silently
# ignored by kustomize — to lift to restricted, add a patches: block
# targeting `name: not-used` (see databases/kustomization.yaml as template)
# AFTER auditing all dev apps for compliance (no privileged caps, runAsNonRoot, etc.).
