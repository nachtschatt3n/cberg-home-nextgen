---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Scaffold references — rename .example.yaml to .sops.yaml AFTER
  # filling in real values + `sops -e -i` encrypting in the destination
  # app dir (CLAUDE.md SOPS rules). Don't deploy this _template dir directly.
  - ./secrets.example.yaml
  - ./ghcr-secret.example.yaml
  - ./helmrelease.yaml
