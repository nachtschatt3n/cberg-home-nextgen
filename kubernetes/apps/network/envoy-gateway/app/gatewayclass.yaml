---
apiVersion: gateway.networking.k8s.io/v1
kind: GatewayClass
metadata:
  name: envoy
spec:
  controllerName: gateway.envoyproxy.io/gatewayclass-controller
  parametersRef:
    group: gateway.envoyproxy.io
    kind: EnvoyProxy
    name: envoy-proxy-config
    namespace: network
---
apiVersion: gateway.envoyproxy.io/v1alpha1
kind: EnvoyProxy
metadata:
  name: envoy-proxy-config
spec:
  provider:
    type: Kubernetes
    kubernetes:
      envoyDeployment:
        # 3 replicas, one per node. Was 1 until 2026-09-07: on that morning a
        # single-replica ingress controller going down with its node took every
        # internal hostname to NXDOMAIN for ~6 min (F-f2b89d20). Envoy had the
        # identical topology, so the cutover would have moved that SPOF rather
        # than removed it.
        replicas: 3
        pod:
          topologySpreadConstraints:
            - maxSkew: 1
              topologyKey: kubernetes.io/hostname
              whenUnsatisfiable: DoNotSchedule
              # DoNotSchedule, not ScheduleAnyway: during a node outage the
              # replacement must stay Pending rather than double up on a
              # surviving node and give us fake redundancy.
              labelSelector:
                matchLabels:
                  app.kubernetes.io/managed-by: envoy-gateway
                  app.kubernetes.io/component: proxy
              # This EnvoyProxy is shared by BOTH gateways via the GatewayClass
              # parametersRef, and both deployments' pods carry the same
              # managed-by/component labels. Without matchLabelKeys the skew
              # would be computed across all 6 pods as ONE group, which permits
              # 2 envoy-internal on one node and 0 on another - the exact
              # concentration this constraint exists to prevent. Keying on the
              # owning gateway makes each gateway spread independently.
              #
              # pod-template-hash is REQUIRED here, not optional polish.
              # Spread is evaluated only at SCHEDULING time and k8s never
              # rebalances afterwards. Without this key, a rolling update
              # counts the outgoing ReplicaSet's pods while they terminate,
              # the scheduler's view shifts underneath it, and replicas pile
              # onto one node - then STAY there. Measured on the first roll of
              # this very change (2026-09-07): both gateways settled 2/1/0,
              # skew=2, in violation of maxSkew=1. Keying on the template hash
              # makes each ReplicaSet spread among its own pods only.
              matchLabelKeys:
                - gateway.envoyproxy.io/owning-gateway-name
                - pod-template-hash
        container:
          # F-41f0d855 (2026-09-22). Envoy Gateway renders the data-plane
          # Deployment from this CR and, with this field unset, takes the image
          # from a constant compiled into the controller
          # (api/v1alpha1/shared_types.go DefaultEnvoyProxyImage at v1.9.1), so
          # nothing in git named the image of the internet-facing data plane
          # and runbooks/check-all-versions.py recorded it NOT MEASURED. This
          # pin is EXACTLY that compiled default -- verified byte-identical to
          # the `envoy` container's spec image and imageID digest on both
          # network/envoy-internal and network/envoy-external before writing,
          # and EG copies the field verbatim (proxy/resource.go
          # expectedProxyImage) -- so it is a naming change, not an image
          # change: no rollout.
          #
          # COUPLED TO THE CONTROLLER. Every EG release compiles in the Envoy
          # it was tested with, and this pin now outranks that default: a
          # gateway-helm bump in helmrelease.yaml MUST move this tag+digest to
          # the new release's DefaultEnvoyProxyImage in the SAME change, or the
          # control plane drives a data plane it never tested. Renovate's
          # built-in kubernetes manager sees this line but docker versioning
          # cannot parse the `distroless-` PREFIX (measured with Renovate's own
          # extractor, see the F-41f0d855 report), so until a regex-versioning
          # packageRule exists this moves by hand, with the chart. Mutually
          # exclusive with imageRepository -- never set both.
          image: docker.io/envoyproxy/envoy:distroless-v1.39.1@sha256:eb2c01c13125d1629637cb4e4cce7207009fb7cc2c8027f9742758549d15b6f4
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              memory: 1Gi
      # Native in EG 1.9.x (provider.kubernetes.envoyPDB). minAvailable 2 of 3
      # so a node drain - including the Talos v1.14.0 roll - can never take the
      # data plane below 2.
      envoyPDB:
        minAvailable: 2
      envoyService:
        # Matches the ingress-nginx controllers we run today: Cilium answers
        # the VIP via L2, and real client IP comes from XFF/CF-Connecting-IP
        # via the ClientTrafficPolicies below, not from source-IP preservation.
        externalTrafficPolicy: Cluster
  telemetry:
    metrics:
      prometheus:
        disable: false
