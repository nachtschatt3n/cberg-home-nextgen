---
apiVersion: v1
kind: Service
metadata:
  name: media-dashboard
  namespace: media
  labels:
    app.kubernetes.io/name: media-dashboard
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: media-dashboard
  ports:
    - name: http
      port: 80
      targetPort: http
