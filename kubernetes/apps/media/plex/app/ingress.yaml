---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: plex
  namespace: media
  annotations:
    external-dns.alpha.kubernetes.io/target: plex.uhl.cool
spec:
  ingressClassName: internal
  rules:
    - host: plex.uhl.cool
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: plex-plex-media-server
                port:
                  number: 32400
  tls:
    - hosts:
        - plex.uhl.cool
