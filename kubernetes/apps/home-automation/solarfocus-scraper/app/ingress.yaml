---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: solarfocus-scraper
  namespace: home-automation
  annotations:
    gethomepage.dev/enabled: "true"
    gethomepage.dev/name: "Solarfocus"
    gethomepage.dev/description: "Solarfocus pellet boiler status"
    gethomepage.dev/group: "Home Automation"
    gethomepage.dev/icon: "solarfocus.png"
    external-dns.alpha.kubernetes.io/hostname: "solarfocus.${SECRET_DOMAIN}"
    # Authentik forward auth annotations
    nginx.ingress.kubernetes.io/auth-url: "http://ak-outpost-solarfocus-scraper-forward-auth.kube-system.svc.cluster.local:9000/outpost.goauthentik.io/auth/nginx"
    nginx.ingress.kubernetes.io/auth-signin: "https://solarfocus.${SECRET_DOMAIN}/outpost.goauthentik.io/start?rd=$escaped_request_uri"
    nginx.ingress.kubernetes.io/auth-response-headers: "Set-Cookie,X-authentik-username,X-authentik-groups,X-authentik-email,X-authentik-name,X-authentik-uid"
    nginx.ingress.kubernetes.io/auth-snippet: |
      proxy_set_header X-Forwarded-Host $http_host;
  labels:
    gethomepage.dev/enabled: "true"
spec:
  ingressClassName: internal
  tls:
    - hosts:
        - "solarfocus.${SECRET_DOMAIN}"
  rules:
    - host: "solarfocus.${SECRET_DOMAIN}"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: solarfocus-scraper
                port:
                  number: 8080
