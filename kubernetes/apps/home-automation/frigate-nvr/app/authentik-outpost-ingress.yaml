---
# ExternalName service to reference the outpost service in kube-system
apiVersion: v1
kind: Service
metadata:
  name: frigate-authentik-outpost
  namespace: home-automation
spec:
  type: ExternalName
  externalName: ak-outpost-frigate-forward-auth.kube-system.svc.cluster.local
  ports:
    - port: 9000
      targetPort: 9000
      protocol: TCP
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: frigate-authentik-outpost
  namespace: home-automation
spec:
  ingressClassName: external
  rules:
    - host: "frigate.${SECRET_DOMAIN}"
      http:
        paths:
          - path: /outpost.goauthentik.io
            pathType: Prefix
            backend:
              service:
                name: frigate-authentik-outpost
                port:
                  number: 9000
  tls:
    - hosts:
        - "frigate.${SECRET_DOMAIN}"

