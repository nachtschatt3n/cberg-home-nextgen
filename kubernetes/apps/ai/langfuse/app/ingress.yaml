---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: langfuse
  namespace: ai
  annotations:
    external-dns.alpha.kubernetes.io/target: "external.${SECRET_DOMAIN}"
    gethomepage.dev/enabled: "true"
    gethomepage.dev/name: "Langfuse"
    gethomepage.dev/description: "Open-source observability for LLM apps"
    gethomepage.dev/group: "AI"
    gethomepage.dev/icon: "invoke-ai.png"
    gethomepage.dev/pod-selector: app=langfuse-web
    # Cookie forwarding for NextAuth.js session management
    nginx.ingress.kubernetes.io/proxy-cookie-path: "/ /"
    # Ensure proper headers for NextAuth.js behind proxy (critical for OAuth state management)
    nginx.ingress.kubernetes.io/configuration-snippet: |
      proxy_set_header X-Forwarded-Proto $scheme;
      proxy_set_header X-Forwarded-Host $host;
      proxy_set_header Host $host;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      # Ensure cookies are preserved for OAuth state management
      proxy_cookie_path / "/; Secure; SameSite=None";
    # Increase timeouts for slow-starting Next.js app
    nginx.ingress.kubernetes.io/proxy-connect-timeout: "60"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "60"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "60"
spec:
  ingressClassName: external
  tls:
    - hosts:
        - "langfuse.${SECRET_DOMAIN}"
  rules:
    - host: "langfuse.${SECRET_DOMAIN}"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: langfuse-web
                port:
                  number: 3000