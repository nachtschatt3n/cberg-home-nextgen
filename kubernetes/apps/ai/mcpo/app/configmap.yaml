---
apiVersion: v1
kind: ConfigMap
metadata:
  name: mcpo-config
  namespace: ai
data:
  config.json: |
    {
      "mcpServers": {
        "context7": {
          "command": "node",
          "args": ["/shared/lib/node_modules/@upstash/context7-mcp/dist/index.js"],
          "env": {
            "CONTEXT7_API_KEY": "${CONTEXT7_API_KEY}"
          }
        },
        "prometheus": {
          "command": "node",
          "args": ["/shared/lib/node_modules/prometheus-mcp/dist/index.mjs", "stdio"],
          "env": {
            "PROMETHEUS_URL": "http://prometheus-operated.monitoring.svc.cluster.local:9090"
          }
        },
        "alertmanager": {
          "command": "uvx",
          "args": ["alertmanager-mcp-server"],
          "env": {
            "ALERTMANAGER_URL": "http://alertmanager-operated.monitoring.svc.cluster.local:9093"
          }
        },
        "kubernetes": {
          "command": "uvx",
          "args": ["mcp-kubernetes-server", "--disable-write", "--disable-delete"],
          "env": {}
        },
        "github": {
          "command": "node",
          "args": ["/shared/lib/node_modules/@modelcontextprotocol/server-github/dist/index.js"],
          "env": {
            "GITHUB_PERSONAL_ACCESS_TOKEN": "${GITHUB_TOKEN}"
          }
        }
      }
    }
