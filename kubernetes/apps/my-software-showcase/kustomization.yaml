---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: my-software-showcase
components:
  - ../../flux/components/common
resources:
  # Namespace-scoped shared objects (ghcr-secret). Every app Kustomization
  # below dependsOn this one, so it is listed first.
  - ./_shared/ks.yaml
  # Core Portfolio Apps — STAGED ROLLOUT (operator go 2026-08-17):
  # Wave 1 canary below; Waves 2/3 are commented until the canary soaks 24h.
  # - ./globalmobility/ks.yaml   # Wave 2/3 — uncomment at its wave
  # - ./haarfabrik/ks.yaml   # Wave 2/3 — uncomment at its wave
  # - ./holm-backend/ks.yaml   # Wave 2/3 — uncomment at its wave
  # - ./ibgastro/ks.yaml   # Wave 2/3 — uncomment at its wave
  # - ./inbewegung/ks.yaml   # Wave 2/3 — uncomment at its wave
  # - ./kfa-medienarchiv/ks.yaml   # Wave 2/3 — uncomment at its wave
  # - ./mangold-smarthomeadvisor/ks.yaml   # Wave 2/3 — uncomment at its wave
  # - ./max-jung/ks.yaml   # Wave 2/3 — uncomment at its wave
  # - ./metaldyne/ks.yaml   # Wave 2/3 — uncomment at its wave
  # - ./ordiga/ks.yaml   # Wave 2/3 — uncomment at its wave
  # - ./see-edv-ibspm/ks.yaml   # Wave 2/3 — uncomment at its wave
  # - ./stepbystepguide/ks.yaml   # Wave 2/3 — uncomment at its wave
  # - ./u-zeit/ks.yaml   # Wave 2/3 — uncomment at its wave
  # - ./uzeit-de/ks.yaml   # Wave 2/3 — uncomment at its wave
  - ./zuhause-betreut/ks.yaml
