---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: my-software-showcase
components:
  - ../../flux/components/common
resources:
  # Namespace-scoped shared objects (ghcr-secret). Every app Kustomization
  # below dependsOn this one, so it is listed first.
  - ./_shared/ks.yaml
  # Core Portfolio Apps — STAGED ROLLOUT (operator go 2026-08-17):
  # Wave 1 canary below; Waves 2/3 are commented until the canary soaks 24h.
  - ./globalmobility/ks.yaml
  - ./haarfabrik/ks.yaml
  - ./holm-backend/ks.yaml
  - ./ibgastro/ks.yaml
  - ./inbewegung/ks.yaml
  - ./kfa-medienarchiv/ks.yaml
  - ./mangold-smarthomeadvisor/ks.yaml
  - ./max-jung/ks.yaml
  - ./metaldyne/ks.yaml
  - ./ordiga/ks.yaml
  - ./see-edv-ibspm/ks.yaml
  - ./stepbystepguide/ks.yaml
  - ./u-zeit/ks.yaml
  - ./uzeit-de/ks.yaml
  - ./zuhause-betreut/ks.yaml
