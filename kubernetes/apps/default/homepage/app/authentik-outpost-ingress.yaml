---
# ExternalName service to reference the outpost service in kube-system
apiVersion: v1
kind: Service
metadata:
  name: homepage-authentik-outpost
  namespace: default
spec:
  type: ExternalName
  externalName: ak-outpost-homepage-forward-auth.kube-system.svc.cluster.local
  ports:
    - port: 9000
      targetPort: 9000
      protocol: TCP
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: homepage-authentik-outpost
  namespace: default
spec:
  ingressClassName: internal
  rules:
    - host: "homepage.${SECRET_DOMAIN}"
      http:
        paths:
          - path: /outpost.goauthentik.io
            pathType: Prefix
            backend:
              service:
                name: homepage-authentik-outpost
                port:
                  number: 9000
  tls:
    - hosts:
        - "homepage.${SECRET_DOMAIN}"

