---
apiVersion: v1
kind: ConfigMap
metadata:
  name: phpmyadmin-config
  namespace: databases
data:
  config.user.inc.php: |
    <?php
    /* Override authentication to use config mode with root credentials */
    $cfg['Servers'][1]['auth_type'] = 'config';
    $cfg['Servers'][1]['user'] = getenv('PMA_USER');
    $cfg['Servers'][1]['password'] = getenv('PMA_PASSWORD');

