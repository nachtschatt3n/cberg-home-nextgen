---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: default
  name: influxdb2-data
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-hostpath
  resources:
    requests:
      storage: 20Gi

