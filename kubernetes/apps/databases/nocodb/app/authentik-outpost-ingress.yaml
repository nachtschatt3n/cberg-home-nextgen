---
# ExternalName service to reference outpost in kube-system
apiVersion: v1
kind: Service
metadata:
  name: nocodb-authentik-outpost
  namespace: databases
spec:
  type: ExternalName
  externalName: ak-outpost-nocodb-forward-auth.kube-system.svc.cluster.local
  ports:
    - port: 9000
      targetPort: 9000
      protocol: TCP

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: nocodb-authentik-outpost
  namespace: databases
spec:
  ingressClassName: internal
  rules:
    - host: "nocodb.${SECRET_DOMAIN}"
      http:
        paths:
          - path: /outpost.goauthentik.io
            pathType: Prefix
            backend:
              service:
                name: nocodb-authentik-outpost
                port:
                  number: 9000
  tls:
    - hosts:
        - "nocodb.${SECRET_DOMAIN}"
