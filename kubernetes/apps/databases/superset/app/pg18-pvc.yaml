---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: superset-pg18-data
  namespace: databases
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
  storageClassName: longhorn-static
  volumeName: superset-pg18-data
