---
# ExternalName service to reference the outpost service in kube-system
apiVersion: v1
kind: Service
metadata:
  name: wazuh-authentik-outpost
  namespace: security
spec:
  type: ExternalName
  externalName: ak-outpost-wazuh-forward-auth.kube-system.svc.cluster.local
  ports:
    - port: 9000
      targetPort: 9000
      protocol: TCP
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: wazuh-authentik-outpost
  namespace: security
spec:
  ingressClassName: internal
  rules:
    - host: "wazuh.${SECRET_DOMAIN}"
      http:
        paths:
          - path: /outpost.goauthentik.io
            pathType: Prefix
            backend:
              service:
                name: wazuh-authentik-outpost
                port:
                  number: 9000
  tls:
    - hosts:
        - "wazuh.${SECRET_DOMAIN}"
