---
apiVersion: v1
kind: Service
metadata:
  name: sweep-dashboard
  namespace: monitoring
  labels:
    app.kubernetes.io/name: sweep-dashboard
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: sweep-dashboard
