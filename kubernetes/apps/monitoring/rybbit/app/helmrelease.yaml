---
apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: rybbit
spec:
  interval: 30m
  chart:
    spec:
      chart: rybbit
      version: 0.4.x
      sourceRef:
        kind: HelmRepository
        name: oci-rybbit
        namespace: flux-system
  install:
    remediation:
      retries: 3
  upgrade:
    remediation:
      retries: 3
  values:
    # Global configuration for bitnamisecure registry
    global:
      imageRegistry: docker.io
      security:
        allowInsecureImages: true

    # Backend configuration
    backend:
      replicaCount: 1
      image:
        repository: ghcr.io/rybbit-io/rybbit-backend
        tag: latest
      betterSecret:
        secretName: rybbit-secrets
        key: BETTER_AUTH_SECRET
      resources:
        requests:
          cpu: 100m
          memory: 256Mi
        limits:
          cpu: 500m
          memory: 512Mi

    # Client configuration
    client:
      replicaCount: 1
      image:
        repository: ghcr.io/rybbit-io/rybbit-client
        tag: latest
      resources:
        requests:
          cpu: 50m
          memory: 128Mi
        limits:
          cpu: 200m
          memory: 256Mi

    # Ingress configuration
    ingress:
      enabled: true
      className: internal
      host: "rybbit.${SECRET_DOMAIN}"
      tls:
        enabled: true
        hosts:
          - "rybbit.${SECRET_DOMAIN}"

    # PostgreSQL configuration - using bitnamisecure registry
    postgresql:
      enabled: true
      image:
        registry: docker.io
        repository: bitnamisecure/postgresql
        tag: latest
      volumePermissions:
        enabled: true
        image:
          registry: docker.io
          repository: bitnamisecure/os-shell
          tag: latest
      auth:
        username: postgres
        database: analytics
        existingSecret: rybbit-secrets
        secretKeys:
          adminPasswordKey: postgres-password
      primary:
        persistence:
          enabled: true
          size: 10Gi
          storageClass: longhorn

    # ClickHouse configuration - DISABLED (not available in bitnamisecure)
    # The backend will need external ClickHouse or can operate without it
    clickhouse:
      enabled: false
      auth:
        username: default
        existingSecret: rybbit-secrets
        existingSecretKey: clickhouse-password
