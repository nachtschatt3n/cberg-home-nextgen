---
# ExternalName service to reference the outpost service in kube-system
apiVersion: v1
kind: Service
metadata:
  name: headlamp-authentik-outpost
  namespace: monitoring
spec:
  type: ExternalName
  externalName: ak-outpost-headlamp-forward-auth.kube-system.svc.cluster.local
  ports:
    - port: 9000
      targetPort: 9000
      protocol: TCP
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: headlamp-authentik-outpost
  namespace: monitoring
spec:
  ingressClassName: internal
  rules:
    - host: "headlamp.${SECRET_DOMAIN}"
      http:
        paths:
          - path: /outpost.goauthentik.io
            pathType: Prefix
            backend:
              service:
                name: headlamp-authentik-outpost
                port:
                  number: 9000
  tls:
    - hosts:
        - "headlamp.${SECRET_DOMAIN}"
